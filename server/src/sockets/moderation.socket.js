// Host moderation socket handlers belong here.
// Events: participant:mute, participant:kick, host:transfer.

export function registerModerationSocket() {
  throw new Error('registerModerationSocket is not implemented yet.');
}
